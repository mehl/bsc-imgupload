services:
  imgupload:
    build: .
    volumes:
      - ./uploads:/data/uploads
      - ./config:/app/config
    env_file:
      - .env
    depends_on:
      - mongodb

  mongodb:
    image: mongo:7
    volumes:
      - ./mongodb:/data/db
    restart: unless-stopped
